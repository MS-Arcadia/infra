[pytest]
# Files run in name order, which is why they are numbered: the suite tells one story, and
# later files depend on what earlier ones published and bought.
testpaths = .
python_files = test_*.py
addopts = -v --tb=short -p no:randomly
markers =
    slow: waits on the saga or a background job
